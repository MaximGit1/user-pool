default:
  asgi:
    host: 0.0.0.0
    port: 8000
    debug: false

  logging:
    level: INFO
    json_format: false

  db:
    debug: false

  cache:
    assignment_ttl: 5400
    client: redis

test:
  asgi:
    debug: true

  logging:
    level: DEBUG
    json_format: false

dev:
  logging:
    level: INFO
    json_format: true
